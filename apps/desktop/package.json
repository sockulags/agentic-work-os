{
  "name": "@awos/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "cross-env CARGO_UNSTABLE_BUILD_DIR_NEW_LAYOUT=false tauri dev",
    "build": "tauri build",
    "ui:dev": "npm --prefix ../.. run dev:ui",
    "ui:build": "npm --prefix ../.. run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
