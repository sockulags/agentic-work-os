node_modules/
dist/
dist-bundle/
*.tsbuildinfo
.env
.env.local
.DS_Store

# The project's own .awos/ is version-controlled: the workspace declaration lives there
# and is meant to be reviewed in pull requests like any other source file. Only the parts
# that belong to one machine or one run are ignored.
.awos/local/
.awos/artifacts/
data/

# tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Vendored build inputs (downloaded, never committed)
.cache/
